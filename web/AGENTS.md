# Web

## Purpose
React 19 + TypeScript + Vite PWA for fleet operations, provider sign-in, recovery and administration.

## Ownership
Owns user interface components, service worker caching, PWA installation manifests, and frontend theme switching.

## Local Contracts
- Web themes default to the Busnes.app cream/light and charcoal/dark palettes with orange accents, following the OS until a browser-local choice is saved. Preserve existing named themes and saved choices.
- Adopted configuration offers on-demand Deployment preflight with 25 services/page, full reported image/target IDs and fixed blocker messages under the heading "Deployment preflight". When `executable` is true it says "Ready to plan: every service maps to an adopted container and its image is known on the host." instead of the blocker list; when it is false with no top-level blockers (only per-service findings) it says "Not ready to plan." Each service row lists its resolved mounts and, under "Will be dropped by the recreate:", the mapped container's mounts the recreate leaves off, and under "Cannot be recreated:" its `unsupported_mounts`; `bind_mount_new`, `mounts_unreported`, `mount_unsupported` and `volume_missing` are fixed texts in `messages`. `clock_skew` and the plan-time blockers (`inspection_unavailable`, `replacement_identity_changed`, `configuration_unsupported`, `frame_too_large`, `too_many_registry_hosts`, `frame_invalid`, `agent_deploy_unsupported`, `agent_pull_unsupported`, `agent_inspect_unsupported`) are fixed texts in `messages`. The live inspection dialog shows "Configuration: fully expressible" or the unsupported configuration by human name from `unsupportedNames` (`ApplicationInspection.tsx`); a code outside that table, a disagreeing `configuration_verified` or a missing `unsupported` list refuses the whole observation. `MountList` (`ApplicationPreflight.tsx`) is the one mount renderer (revision, preflight, plan): kind label `volume`/`bind` from a fixed table (anything else "other"), `source → target` as text, `ro` badge. Mapped services offer a single native modal live inspection using the authorized endpoint API. Compare container/image/creation identity to the preflight target; display only validated redacted facts, discard results on close/scope refresh, abort dismissed requests and never retry automatically. Keep running-container image separate from desired image; selected facts do not establish configuration parity. IDs are observations, not deployment pins; port checks cannot detect host processes or unreported bindings. Hide observations if the selected adoption changed. No apply action.
- Adopted configuration offers an on-demand Deployment plan panel (`ApplicationDeploymentPlan.tsx`), gated on the deployments read alone; only the plan form beneath it additionally gates on the mapping read. Typed project confirmation posts instance/mapping-version/revision to mint a plan, 25 services/page showing pinned image ID and repository digest (or "No repository digest reported"), or for a service the apply pulls only "pulls " + 12 hex characters of `pull_digest` (never the image it replaces), replaced container/image identity, each service's mounts and secret reference counts only; "Volumes to ensure:" lists the plan's resolved host volume names. States expiry and that planning again replaces the earlier plan; always states nothing is executed here. A `Revision to plan` selector offers `latest_revision..1` (default latest) and posts the chosen `revision`; below latest it shows the fixed reapply warning (own saved environment values, data written since not reversed, mapping reviewed against the latest). The panel shows the instance's current and previous revision (0 as "none") and a "Deployment history" table of the instance's rows newest first (25/page: kind, revision, state, applied by, applied, settled) with a per-row "Show steps" toggle reusing the result section. A `remove` row's plan renders its container list (service, name, container ID) instead of the services table; its progress uses the same polling and result section, and a denied removal precondition advises refreshing the inventory and re-adopting a container recreated outside KyYard rather than planning again. An application with no instance (removed or released) offers a read-only "Deployment history" toggle (`ApplicationHistory`) listing all its rows. `knownBlockers` (`ApplicationPreflight.tsx`) is the one parser for a 409 `blockers` list. `409` blockers map through the shared preflight `messages`; any other blocked/unknown outcome disables re-planning until refresh. A 409 also lists `serviceFindings`: per named service (service grammar only) its unsupported configuration by human name, or "no live inspection answered". A `recheck` denial is explained like a precondition keyed on its step code; a `clock_skew` result explains the host clock (see the Apply deployment bullet for the outcome-code mechanism).
- A mapped instance (`mapping_version >= 1`) shows an "Updates" toggle (`ApplicationUpdates.tsx`) between preflight and the deployment plan; like its siblings it requests nothing until opened. Open, it reads `GET .../applications/{app}/updates` (rows only when `instance_id` matches the instance; "No update check yet." when empty) and "Check for updates" posts `POST .../updates/check`, then re-reads, keeping the previous rows on screen until the read lands. Rows show service, reference, a fixed verdict badge (Up to date, Update available, Pinned, Unknown on host, Registry error), local and registry digests shortened to 12 hex characters, and the check time; a `registry_error` shows a fixed text per known detail and nothing for any other. `tenantWrite` maps 403 and 409 `check_in_progress` ("An update check or update plan is already running."), `mapping_required`, `adoption_changed` to fixed texts through its `conflict` table. When any shown row is `update_available`, a typed project confirmation (`project` prop) enables "Plan update", which posts `POST .../deployments` `{instance_id, mapping_version (from the check), revision (latestRevision prop), confirm, update: [those services]}`; success shows "Plan created; review it in the deployment plan panel." and calls `onPlanned`, which `Applications.tsx` turns into a `refreshKey` bump that remounts the open plan panel. A 409 `blockers` list maps through `knownBlockers` over the preflight `messages` plus `update_not_mapped` and `registry_<detail>` (the same texts as the registry-error details); 403, 429 `too_many_checks`, 409 `adoption_changed` ("Adoption changed. Refresh applications before planning."), 409 `deployment_in_progress` ("A deployment is being applied; wait for it to finish.") and 409 `check_in_progress` ("An update check or update plan is already running.") have fixed texts; anything else shows one fixed refusal text. No apply action here.
- An adopted or draft application's configuration shows an "Update policy" toggle (`ApplicationPolicy.tsx`) that requests nothing until opened, then reads `GET .../update-policy` (404 is "No update policy."). The status line shows mode, days, window and zone, then "Active. Next window: …" in the policy's zone and the browser's (`formatIn`, falling back to UTC for a zone the browser cannot format), or "Paused." with the reason's fixed text; the run list shows window, a `RUN_OUTCOMES` badge (unknown: "Unrecognised outcome"), a detail rendered only through fixed tables (`planBlockers` per comma-separated blocker, `detailText` and the store codes, the scheduler's sentences; anything else nothing) and the deployment's first 8 characters (full ID in `title`). Only `organization_admin` (`canManagePolicies`, from `/api/organizations` in `Applications.tsx`) sees the editor, Delete (confirmed) and Resume: mode, weekday checkboxes, start and end `<input type="time">` (end 00:00 is 1440), and a zone `<select>` from `Intl.supportedValuesOf('timeZone')` that always keeps the saved and the browser's zone (`zoneChoices`); an engine without `Intl.supportedValuesOf` (`hasZoneList`) gets only the saved and browser zones in the list plus a free-text IANA zone input, validated server-side. The editor refuses no day and a window under 15 minutes or reversed before sending; 400 codes and 403/404/409 `policy_not_paused` map to fixed texts. Each run shows its deployment's validation (`ApplicationValidation.tsx`: `VALIDATION_VERDICTS`, the failing service's name when it has the service shape, the loop's sentences, "Rollback sent to revision N" (outcome `applied`: the rollback was sent; the JSON carries no rollback state, so its settle shows on its own deployment row) with the rollback deployment's first 8 characters in `<code title>`, or "Not rolled back" with `ROLLBACK_REASONS`/`planBlockers` text; unknown codes dropped). A pause the validation loop caused is explained by `pauseText`. With the adopted instance's endpoint known and the policy in `apply` mode, the card reads `GET /api/organizations/{org}/endpoints/{endpoint}` and warns when `container.inspect.health` is missing. The deployment plan's result shows the same `ValidationLine`.
- A `planned`, unexpired plan offers "Apply deployment": a second typed confirmation naming the application, revision and endpoint states that containers will be replaced and nothing rolls back. On `202` the panel polls `GET .../deployments` directly (never through the shared resource, which would unmount the panel) every 5 s while state is `applying`, until three minutes past the row's `deadline` (15 minutes from the first tick when it has none) or 3 consecutive fetch failures, then shows "Status updates paused; refresh to continue." A settled row shows state, its correlation ID ("search the audit log for it"; a planned row shows it under the plan), the row detail only when it is one of the server's own sentences (`SERVER_DETAILS`: connection ended, no result arrived, the host's result did not match, the endpoint disconnected before the deployment or removal was sent), the result code's text (`RESULT_CODES`), a 25-row step table (service, step, outcome, `stepText`: the step code's text from `STEP_CODES`, with `unsupported` codes named through `unsupportedNames` and unknown ones dropped, a 64-hex container ID for `identity_unreadable`/`identity_unverified` and a 3-digit status for `runtime_status`, each rendered only in that shape) and the new container identities. `legacy` reads "The agent did not classify this outcome; upgrade the agent."; any other unknown code renders as the inert text ``unrecognised outcome `<code>` ``. Explanations key on the row state first: `unknown` (host may or may not have acted), `timed_out`, and `failed` with no result (never sent to the host); a `clock_skew` result; then on the failing step's code: `denied` at `precondition` or `recheck` (`configuration_drift`, `container_missing`, `identity_mismatch`, `image_missing`, `legacy` says nothing more, otherwise configuration the definition cannot describe), `pinned_image_missing`, and `failed` (previous container renamed `.kyyard-prev-<id>` on the host). The panel states that planning executes nothing; it does not call an applyable plan inert. `501`, `403` and named `409` codes (`deployment_in_progress`, `adoption_changed`, `endpoint_offline`, `deployment_not_sent`) map to fixed messages; error bodies are never rendered. No retry, no rollback control.
- Adopted configuration offers “Map services to containers”: explicit owned-ID choices, at most one container per service and one service per container, unmapped option, 25 services/page, full container/image identity and typed project confirmation. Never auto-select from labels. Submit reviewed instance/version/digest and replace the whole map. Show definition-revision mismatch; removed service assignments clear only on save. Failures/unknown outcomes block repeated save until refresh/review; instance replacement requires Applications refresh.
- Application configuration has a saved-revision selector (each service shows image, restart, ports, volumes and environment key names) and a separate full-definition “Save new revision” form. Always submit the observed latest head and all Compose values; do not prefill plaintext secrets or silently copy omissions. Confirm the named application/revision and no runtime effect. Clear sensitive input on success/cancel/scope change. Conflict/unknown outcomes block further saves until Applications refreshes; cancel/reopen alone cannot reset that block.
- Adopted application configuration offers on-demand “Compare with host”. Show desired/adopted revision separately, inventory receipt time, explicit unknown/unavailable states, immutable ownership differences and advisory service/image-reference observations. Paginate services/containers at 25 rows; hide comparison if its instance differs from the selected adoption. Never describe matching image references as matching image content, or missing labels as absent services.
- Container rows use a compact native Actions disclosure. Port mappings group matching host/container/protocol tuples while retaining every reported bind address, and distinguish unpublished ports. Select pickers use full-width themed option highlights where customizable native selects are supported; older browsers retain native controls.
- Applications distinguishes drafts from adopted snapshots. Adoption selects an authorized environment host/project, shows the server preview (25 containers/page) and requires typed exact project confirmation; POST carries its digest. Unknown/refused writes block repeated submission until refresh/review. Release confirms readable application, host and project and submits the exact instance ID. An adopted instance also offers "Remove application": fixed text naming the containers stopped/removed and what is kept (named volumes, images, saved revisions), typed project confirmation, POST `removal` `{instance_id, confirm}`; `202` refreshes Applications and shows "Removal sent; watch Deployment history for progress."; 403, 501 and 409 codes (`endpoint_offline`, `deployment_planned`/`deployment_in_progress`, `deployment_not_sent`, `adoption_changed`, `removal_too_large`) and `preflight_blocked` blockers (`unadopted_project_containers`, `apply_outcome_unknown`) map to fixed texts and clear the confirmation so the operator may retry; only 5xx or network failure (unknown outcome) blocks until refresh; bodies are never rendered. An application with `removed_at` is labelled "Removed {date}", may be discarded, and offers only the adoption panel (re-adoption revives it); mapping, preflight and plan panels appear once it has an instance. Switching application/host/project discards its preview. Ownership-read failure must not label projects unmanaged or enable draft discard. Adopted project labels describe the saved snapshot; only recorded immutable IDs are marked adopted, and replacement containers are not silently claimed.

- Environment resources separate Hosts and Applications. Applications lists paginated saved drafts, imports a bounded explicit Compose subset, inspects reference-only configuration, and confirms discard at the observed revision. Import never deploys/adopts. Keep YAML only in component memory; clear on success, explicit clear, resource switch or scope change. Unknown writes disable further changes until the operator checks drafts; diagnostics never echo source/server text.
- Use the Ky palette system with a desktop sidebar and compact mobile navigation. Operational content comes before administration. Single-membership installations automatically use their authorized scope and show no organization name or selector; existing multi-membership accounts retain explicit access-scope switching. Visible administration uses Environments, Members and Audit history. Backend tenant IDs, routes and authorization remain intact. Keep theme controls in Settings. Shared spacing, focus styles, responsive tables and bounded dialogs live in `styles/theme.css`.
- Home exposes existing container actions directly, retaining server authorization, observed identity, confirmation and CSRF. Host pages default to Containers and separate Projects, Images, Networks, Volumes, Activity and Details with explicit resource buttons. Container/image text and controls must remain reachable at mobile widths; tables become labeled rows rather than forcing actions offscreen.
- Logs open in a native modal dialog; Escape/close/unmount releases the reader. Resource-tab changes unmount their controls. Key environment/endpoint pages on full route scope so enrollment tokens, filters and open streams cannot follow navigation to another host/environment.
- Settings separates Appearance, Sign-in, Recovery and Storage, plus Administration when `/api/auth/me` reports `user.role === "admin"` (`App.tsx` passes the role). Load provider configuration only when Sign-in is selected. Environment audit/member administration is reached through links rather than embedded under everyday host controls.
- Endpoint Compose discovery groups only the current authorized container snapshot by exact project name. Projects show recorded adoption separately from discovery; expanding/filtering makes no request or runtime mutation. Show observed running/container counts, preserve snapshot freshness and partial-inventory warnings, and reset the filter across endpoint scope changes or when a refreshed project disappears. Render project identity in full as isolated text; arbitrary labels/configuration are not part of project inspection.
- Container controls offer Terminal only when `canExec` is true: the pages pass `canExec(role)` (`tenant.ts`, `organization_admin` only, mirroring `permissions.Allows(role, ContainerExec)`) with the role from `GET /api/organizations`; no membership means no Terminal. It opens a native modal dialog (focus trap, Escape/close disconnect) with an explicitly confirmed terminal with a required container user and shell executable. The terminal emulator (`@xterm/xterm`, lazily loaded) sends the CSRF token in its first WebSocket message, never in URLs, and binds every later frame to the returned stream ID. No automatic reconnect or execution retry. Input/output chunks are 32 KiB; display writes and socket backlog are bounded to 256 KiB and scrollback to 1,000 lines; overflow disconnects. Unmount/context change releases the socket and emulator. Resize controls bound rows/columns to 1–512. Container escape sequences cannot open links or set application HTML, titles or clipboard contents. Exit status is distinct from an unknown attachment close.
- Home-page guidance presents local Docker as automatic in the standard installation; manual enrollment is for additional hosts. Empty inventories with no runtime version report Docker unavailability and socket/service guidance rather than claiming the host has no containers.
- Endpoint image controls pull explicit tags/digests and remove immutable image IDs after a fresh server-inventory preview and full-ID confirmation. Stale/incomplete dependency data or known container dependents block the preview. The agent rechecks before deletion; no force or automatic retries. Unknown submissions/results remain blocked until the operator checks activity and refreshes. Image controls take no registry credentials; those live only in the Registries panel.
- Home lists one selected host’s container inventory with 25 rows per page, search across its full reported snapshot, host selection and host pagination (20/page). Host resource tables also page at 25 rows. Reset paging on search/project/scope changes and clamp when inventory shrinks. Preserve per-host error/freshness states, direct container controls and endpoint links. Action confirmations include the sanitized human-readable host name and endpoint ID. Pagination is shown only when needed. `/endpoints` lists hosts; environment enrollment appears before environment settings. No SCIM or mobile-pairing UI remains.
- Settings offers provider configuration through admin-only `/api/settings/sso`, callback URLs from the server's advertised origin, and the shared theme swatches. Login renders only the public provider list.
- Endpoint container controls use immutable IDs and observed image/state preconditions. Removal first fetches the server preview and requires the full observed name. Commands are never retried automatically; recent activity remains accessible for unknown outcomes.
- Container logs support bounded reads, search, timestamps, download and SSE follow. Browser display retains 256 KiB; follow closes on error without automatic reconnection, and unmount/filter changes cancel the reader. Server notices are separate from container output during follow.
- A signed-in user with `must_change_password` sees only password replacement and sign-out. Replacement uses `secureFetch`, returns to login after session revocation, and never exposes the normal navigation before completion.
- Login shows local setup/SSH/TLS guidance for HTTP installations and offers SSO only when enabled.
- Site branding uses Lucide’s shipping-container outline in the header, login, favicon and install icons; retain its rounded line-art style and theme color inheritance inside the app. Static icon assets carry the Lucide license and are rasterized from the same geometry.
- Product title, PWA name, and pre-settings fallback name are `KyYard`.
- Strict TypeScript type safety without unused imports.
- `theme.ts` carries the fifteen KyPost/KyDNS palettes. Settings shows visual swatches; header and login have no upper-right theme dropdown, by user preference. Apply before React renders, persist under `kyyard-theme`, synchronize theme changes across tabs, and never write a personal theme to instance settings.
- Authenticated state-changing requests use `secureFetch` so the `ky_csrf` cookie is mirrored into `X-CSRF-Token`.
- Navigation is path-based (`src/router.ts`, no router dependency): `/`, `/endpoints`, `/backup`, `/settings`, `/organizations/{org}`, `/organizations/{org}/members`, `/organizations/{org}/audit`, `/organizations/{org}/environments/{env}`. Segments are limited to 64 safe characters; anything else is the not-found screen. `Link` renders real anchors with `aria-current` and leaves modified clicks to the browser. Deep links survive sign-in because the path is untouched while `Login` is shown, and the service worker serves the app shell for offline navigations.
- Access-scope switching lists only the caller's own memberships from `GET /api/organizations` and re-reads when the current organization changes. `<main>` is keyed on the organization ID, so every tenant screen's state is discarded on context change; there is no client-side tenant cache.
- Tenant screens read through `useTenantResource` and show exactly one of loading / denied (401, 403) / not found (404) / offline (network failure) / error, with a retry where retrying can help. Writes go through `tenantWrite` on `secureFetch`; `409 last_administrator`, 403, 404, 409 and network failure map to fixed user-facing messages, and a screen may pass its own 403, 400 and 404 texts and a `conflict` table of 409 codes; any other failure shows only the status code, never server text. A write that needs the success body calls `secureFetch` itself and maps a refusal through `refusal` (the same table). Malformed percent-escapes and dot segments in the path land on the not-found screen.
- `/organizations/{org}/endpoints/{endpoint}` (`pages/EndpointPage.tsx`) shows the endpoint's facts, key, capabilities and the stored inventory (containers, images, networks, volumes) with counts; "no inventory yet" (404) is distinct from an empty host; the status line names the generation, how long ago the server received it, flags staleness past three minutes, clock skew past five minutes, and any truncated lists. The containers table carries a Usage column from the latest sample per container: a running container with no sample reads "no data", a first sample with no CPU interval reads "cpu —", and a stopped container a dash; zero is shown only when it was measured. Endpoint names in the environment list link to it.
- The environment screen owns endpoints (`components/Endpoints.tsx`): "Enroll a host" mints a token through `secureFetch` and shows the returned installation command, its scope/restart note, expiry and Docker-socket disclosure once, never refetching it; a details section retains the token for source installs. HTTPS installations get one remote Docker run/link command; HTTP-only installations explain the required reachable HTTPS address. Refresh hosts reloads pending enrollments after the operator starts the agent; endpoint names pass through `displayName` (control characters stripped, clamped to 64) before any confirmation text; approve confirms with the full fingerprint and posts exactly that fingerprint; reject and revoke confirm their consequences; terminal states (`revoked`, `expired`) show no actions. A pending rotation shows both fingerprints (approved and pending) and an "Acknowledge rotation" button whose confirmation carries both in full; other high-severity alerts (`duplicate_connection`) render with their details and a Clear button. `409 environment_in_use` maps to a message telling the operator to revoke endpoints first.
- The environment's enrollment has a runtime selector (Docker host / Kubernetes cluster). A cluster needs a name, and a namespace list that passes `validNamespaces` (the server's rule: at most 32 distinct DNS-1123 labels, not `kyyard-agent`, not `kube-*`; otherwise the field is `aria-invalid` and `NAMESPACE_RULE` shows as an alert), before "Enroll a cluster" enables; it posts `{runtime: "kubernetes", name}`, plus `namespaces` when the optional "Namespaces to deploy to" field lists any (split on commas and spaces) and shows, once, the `kubectl apply -f <manifest_file>` command, the manifest (collapsed), note and RBAC disclosure, with Download manifest (a Blob named `manifest_file`) and Copy manifest; 409 `agent_image_unpinned` and `https_required` map to fixed texts. The Docker one-liner is unchanged. Endpoint rows show a Kubernetes endpoint's `cluster_health` badge (`healthBadge`, shared with `KubernetesCluster.tsx`) and node count.
- `/organizations/{org}/endpoints/{endpoint}` for a `kubernetes` endpoint renders `KubernetesCluster.tsx` under the `Cluster` tab (with `Details`): cluster health and nodes, a namespace filter (falls back to all when the namespace leaves), workloads (ready/desired, images), pods (phase, node, summed restarts, containers with state and a Logs action opening the shared `ContainerLogs` dialog against `/pods/{namespace}/{pod}/logs` with `container`), services and volume claims, and an Applications panel naming the namespaces the manifest grants and each mapped application's project, namespace and ready Deployments (from `Workload.instance`), with Regenerate manifest for organization and environment admins (`canEnroll`). No container, image, network, volume, exec, inspect or update control is rendered; the status line's truncation notice covers the cluster lists. `ResourceTable.tsx` is the shared paged table.
- An application without an instance offers, below the Docker adoption (whose host list leaves clusters out), `KubernetesMapping`: a cluster select (Kubernetes endpoints only; the card is absent without one), a namespace select from the cluster's `deploy_namespaces`, "Save namespace mapping" (`PUT .../mapping {endpoint_id, namespace}`, fixed sentences for `namespace_unknown`, `application_adopted`, `deployment_in_progress`, `runtime_unsupported`, 403 and other 409s), and for admins `ManifestRegeneration` beside it (`POST .../endpoints/{endpoint}/manifest` with the typed list, then the command, the collapsed RBAC manifest, the note, Download manifest and Copy manifest with a Copied/failed status line). A Kubernetes instance shows its namespace, the same card (cluster fixed, namespace changeable), `KubernetesWorkloads` (its labelled Deployments from the cluster inventory, ready/desired, images, and the one sentence that health is not validated and nothing rolls back) in place of the Docker service mapping and comparison, and removal worded as deleting the labelled objects. Plans show `Deployment <namespace>/<name>` for each service and the apply text for a namespace; step codes `forbidden`, `pod_security` (one sentence per detail: `missing`, `privileged`, `invalid`; label the namespace baseline or restricted), `rollout_timeout` (reasons as words, `replicafailure` included), `conflict`, `name_taken` and `admission_denied` (quota or policy; `Kind/name`) render from the table in their closed shapes only; an apply refused `409 preflight_blocked` reads its known blocker sentences (`k8s_namespace` for a namespace revoked since the plan); blockers `kubernetes_unsupported` and `k8s_namespace` and the `k8s_` codes read as sentences naming the fix; history lists Deployment identities; a cluster row's explanation reads `CLUSTER_STOPPED` for a precondition refusal and `CLUSTER_FAILED` (or the removal sentence) for a failed step, never the Docker `.kyyard-prev` text; a Kubernetes apply's unverifiable validation reads `KUBERNETES_UNVERIFIED`, not the upgrade advice.
- Under each instance, `ApplicationMigration` reads `GET .../migration`. An organization administrator (`canMigrate`, mirroring `application.migrate`) on a Docker instance with none sees "Migrate to a Kubernetes cluster": cluster and namespace selects (clusters and their `deploy_namespaces` only; absent without a cluster) and "Analyze" (`POST .../migration`). An open migration shows the report as one table (service, axis, class badge, `findingText`: the fixed sentence of `MIGRATION_CODES` plus its parameter, an `UnsupportedCodes` detail by its name, `inspection_unavailable` with detail `agent` as `INSPECTION_AGENT` (upgrade the Docker agent); unknown codes render nothing), the assumptions, the choices form while `analyzed` (a checkbox per reported code of `ACKNOWLEDGEMENTS`, which mirrors `store.MigrationAcknowledgeable`; per named volume: a StorageClass select from the destination inventory's `storage_classes`, "Cluster default (<name>)" only while a default is reported; a size; access mode fixed `ReadWriteOnce`; "Save choices" sends `PUT .../choices` `{volumes, acknowledged}`), "Analyze again", "Create destination" (disabled until `ready`), "Open <destination>" (selects the destination in the list), the checklist (`CHECKLIST_STEPS` with each step's commands), the two confirmations each with a required note, and "Abandon migration" (confirmed; the destination-kept sentence when `destination_kept`). A destination shows "Migration destination of <source> · <status>" and "Open <source>". Refusals map through `MIGRATION_ERRORS`; server text never renders, and a response that is not a migration renders nothing. Plans show their claims and claim mounts, `claim_immutable` names its `PersistentVolumeClaim/<name>`, a removal's kept claim reads `CLAIM_RETAINED`, a cluster removal says its claims are kept and its result lists them under "Kept on the cluster" (`KeptClaims`: each `retained_claims` name, and when the agent reported more retained steps than names, their count with `kubectl -n <namespace> get pvc -l kyyard.busnes.app/instance=<instance>`), a `rollout_timeout` detail's `claim=Pending` or `claim=Lost` appends its `CLAIM_PHASES` sentence, history marks `Apply (migration)`, a `k8s_volume` with detail `choice_required` reads `K8S_VOLUME_CHOICE` (choices carry over between revisions; a newly declared volume has none), and the plan blockers `agent_claims_unsupported` and `storage_class_unknown` read their `messages` sentences. `ApplicationMigration.test.tsx` compares `MIGRATION_CODES`, `CHECKLIST_STEPS`, `ASSUMPTIONS`, `ACKNOWLEDGEMENTS`, `STEP_CODES` and `unsupportedNames` with the generated `migration-codes.json` and `protocol-codes.json` exactly.
- Every show/hide toggle button (the `setOpen(!open)` panels, the container Logs and Terminal buttons) sets `aria-expanded` to its open state.
- The members screen (`/organizations/{org}/members`) renders `components/Registries.tsx` below the member list: rows show host, name, username, credential set/none and the private-address flag; one add/update form (`PUT .../registries`) whose `type="password"` credential is never prefilled and is emptied after every submit. An empty credential omits the key (server keeps the stored one), "Clear credential" sends `""`, The allow-private checkbox renders only when the policy read reports `private_registries_enabled`; otherwise the form shows "Private-address registries are disabled by the operator (KY_REGISTRY_ALLOW_PRIVATE)." and sends `allow_private: false`, and `tenantWrite` maps code `private_registries_disabled` to that text. Edit fills only host/name/username/allow-private, shows "Editing <host>" and a Cancel that resets the form. Delete confirms with `window.confirm`. The anonymous-pull switch (`PUT .../registry-policy`) carries the fixed Off/On explanation and confirms only off-to-on. Writes use `tenantWrite` with its 403 ("Only an organization administrator can manage registries.") and 400 (host, name, credential at most 4096 bytes) overrides. Controls render for every role; the server refuses non-admins.
- Settings → Administration (`components/Administration.tsx`) lists `/api/admin/organizations` (name, members, created) and `/api/admin/users` (username, display name, role, status, provider). Create organization posts `{name, admin_user_id}`, the first admin chosen from active users only, and shows "Organization created. Its admin adds members on the organization page." Create user posts `{username, display_name, role}` (`user` or `admin`), clears the form, and shows the returned temporary password once in a read-only field with Copy and "Give this password to the person out of band. They must change it at first sign-in. It is not shown again." 403 is "Administrator role required."; 409 `organization_exists`, `user_inactive`, `username_exists`, 404 and 400 map to fixed texts.
- `Backup.tsx` warns for as long as `database_driver` from `/api/backup/status` is not `sqlite`: only the SQLite path can snapshot a database into a capsule, so a Postgres deployment makes no capsules at all.

## Verification
- `make test-web` or `cd web && npm ci && npm test`, then `npm run build` (vitest with jsdom; `src/pages/Backup.test.tsx` renders the recovery screen against a stubbed status route; `src/pages/Organization.test.tsx` covers denied/offline/retry states, CSRF on tenant writes, the last-administrator message and deep links with browser history; `src/components/Endpoints.test.tsx` covers the one-time enrollment command and fingerprint-bound approval, the Kubernetes runtime switch, manifest Download/Copy, the fixed 409 texts and the cluster row badge; `src/pages/EndpointPage.test.tsx` covers the read-only cluster view (no Docker tab or control, namespace filter, pod logs on the pod route); `src/components/ApplicationUpdates.test.tsx` covers each verdict badge, fixed registry-error texts, the CSRF-bearing check and its fixed 403/409 texts, the panel hidden without a mapping, no request before it is opened, previous rows kept during the post-check read, the Plan update body and `onPlanned`, every update blocker, 429 and the plan 409 codes as fixed text; `src/components/Administration.test.tsx` covers the admin-only tab, both tables, the active-only first-admin picker, CSRF-bearing creates, the one-time temporary password with Copy, form clearing and fixed refusal texts; `src/components/Registries.test.tsx` covers the credential key omitted/set/cleared, the field emptied after a failed save, edit/cancel, delete confirmation, the confirmed policy switch, fixed 403/400 texts and both operator private-registry states; `src/components/ApplicationPolicy.test.tsx` covers the unopened card, the admin-only editor, client validation, the PUT body with CSRF and 00:00 as 1440, every 400 code as fixed text, a saved zone the browser does not list, runs through fixed tables, pause and resume, and the next window in both zones; `src/components/ApplicationValidation.test.tsx` covers every verdict, the service and sentence details, the rollback line and every reason, and the validation pause texts). Commit `web/dist` after a build; CI diffs it.

## Shared browser UI

- `src/ky-ui/` is generated from Busnes-app/ky-ui, pinned by `VERSION` file hashes. Change shared colors, navigation states and storage helpers upstream, then run its consumer sync with an explicit worktree map; do not hand-edit vendored files.
- Products own layout, routes, saved choice keys and named palettes. Busnes aliases consume shared tokens; mark primary navigation with `ky-nav-item` while preserving current-page semantics.
- Verify vendored files with `node src/ky-ui/check-vendor.mjs` from this document's directory. Builds/CI run that check. Rendered evidence and capture limitations are recorded in the repository-root `UI-VERIFICATION.md`.

## Child DOX Index
None.
